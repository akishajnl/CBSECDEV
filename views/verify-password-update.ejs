<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GapFinder | Verify Identity</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        body { background-color: #f8f9fa; font-family: 'Poppins', sans-serif; color: #333; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .gf-card { background: white; border-radius: 15px; padding: 40px; box-shadow: 0 10px 30px rgba(0,0,0,0.05); border: 1px solid #f0f2f5; width: 100%; max-width: 450px; border-top: 4px solid #00aeef; }
        .brand { font-family: 'Montserrat', sans-serif; font-size: 1.8rem; color: #162622; font-weight: 700; text-align: center; margin-bottom: 25px; display: block; }
        .brand span { color: #00aeef; }
        .btn-custom { background: #00aeef; color: white; font-weight: 600; padding: 10px; border-radius: 8px; width: 100%; border: none; transition: 0.3s; }
        .btn-custom:hover { background: #0095cc; color: white; }
    </style>
</head>
<body>

    <div class="gf-card">
        <a href="/" class="brand">Gap<span>Finder</span></a>
        <h5 class="fw-bold mb-3 text-center" style="font-family: 'Montserrat', sans-serif; color: #162622;">Security Verification</h5>
        <p class="text-muted text-center mb-4" style="font-size: 0.9rem;">Please answer your security question to proceed with changing your password.</p>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger py-2" role="alert" style="font-size: 0.85rem;"><%= error %></div>
        <% } %>

        <form action="/auth/security/verify-answer" method="POST">
            <div class="mb-3">
                <label class="form-label fw-medium text-dark" style="font-size: 0.9rem;"><%= user.securityQuestion %></label>
                <input type="text" class="form-control" name="securityAnswer" placeholder="Your answer..." required autofocus>
            </div>

            <button type="submit" class="btn btn-custom mb-3">Verify Answer</button>
            <div class="text-center">
                <a href="/profile/security" class="text-muted text-decoration-none" style="font-size: 0.85rem;"><i class="fa-solid fa-arrow-left me-1"></i> Cancel and Return</a>
            </div>
        </form>
    </div>

</body>
</html>